apiVersion: v1
kind: Service
metadata:
  name: flask-otel
  namespace: observability
spec:
  type: NodePort
  selector:
    app: flask-otel
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30500
